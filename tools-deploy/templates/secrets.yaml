{{- range $name, $data := .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $name }}
  namespace: {{ $data.namespace }}
type: Opaque
data:
{{- range $key, $val := $data }}
  {{- if not (eq $key "namespace")}}
  {{ $key }}: {{ $val | quote }}
  {{- end}}
{{- end }}
---
{{- end }}
